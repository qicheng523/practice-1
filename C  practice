#include<stdio.h>
int main()
{
	printf("请输入数字：");
	int number;
	scanf("%d",&number) ;
	int digit,b;
	int a=0;
	while(number>0)
	{
		digit=number%10;
	    a=a*10+digit;
	    number/=10;
	    printf("number=%d,a=%d,digit=%d\n",number,a,digit);
	}
	printf("数字逆序为：%d",a); 
	return 0;
}
